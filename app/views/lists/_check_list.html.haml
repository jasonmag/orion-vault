%form{ action: "#", method: "post" }
  %ul
    - @lists.each do |list|
      %li.flex.items-center.mb-2
        %input{ type: "checkbox", name: "selected_lists[]", value: list.id, id: "list_#{list.id}" }
        %label.ml-2{ for: "list_#{list.id}" }
          %span.font-medium= list.name
          %span.mx-2.text-gray-500 | |
          %span= list.price
        -#   %span.mx-2.text-gray-500 | |
        -#   %span= list.description
        -#   %span.mx-2.text-gray-500 | |
        -#   %span= list.due_date
        -#   %span.mx-2.text-gray-500 | |
        -#   %span= list.user.email
        %div.ml-auto.flex.space-x-2
          = link_to 'Show', list, class: 'text-blue-500 hover:text-blue-700'
        -#   = link_to 'Edit', edit_list_path(list), class: 'text-green-500 hover:text-green-700'
        -#   = link_to 'Destroy', list, method: :delete, data: { confirm: 'Are you sure?' }, class: 'text-red-500 hover:text-red-700'

  %br
-#   %button.bg-blue-500.text-white.py-2.px-4.rounded{ type: "submit" } Submit Selected
